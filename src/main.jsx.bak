import React from "react";
import ReactDOM from "react-dom/client";
import { BrowserRouter, Routes, Route, Link } from "react-router-dom";
import "./index.css";
import Home from "./pages/Home.jsx";
import Ask from "./pages/Ask.jsx";

function Nav() {
  return (
    <header className="max-w-5xl mx-auto px-4 py-4 flex items-center justify-between">
      <Link to="/" className="text-xl font-semibold">Bayesian Sapien</Link>
      <nav className="flex flex-wrap items-center gap-4 text-sm">
        <a href="/resume.pdf" className="hover:underline">Resume</a>
        <a href="https://www.linkedin.com/in/your-handle" target="_blank" rel="noreferrer">LinkedIn</a>
        <a href="https://twitter.com/your-handle" target="_blank" rel="noreferrer">Twitter</a>
        <a href="https://yourblog.example.com" target="_blank" rel="noreferrer">Blog</a>
        <a href="https://github.com/your-handle" target="_blank" rel="noreferrer">GitHub</a>
        <Link to="/ask" className="px-3 py-1 rounded bg-white/10 hover:bg-white/20">Ask me</Link>
      </nav>
    </header>
  );
}

function App() {
  return (
    <div className="min-h-screen text-slate-100" style={{background:"#0b1220"}}>
      <Nav />
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/ask" element={<Ask />} />
      </Routes>
      <footer className="max-w-5xl mx-auto px-4 py-10 text-xs text-slate-400">
        © {new Date().getFullYear()} Bayesian Sapien · bayesiansapien.tech
      </footer>
    </div>
  );
}

ReactDOM.createRoot(document.getElementById("root")).render(
  <BrowserRouter>
    <App />
  </BrowserRouter>
);
